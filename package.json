{
  "name": "figure-shop-workspace",
  "private": true,
  "scripts": {
    "dev": "concurrently -n client,server -c green,blue \"npm --prefix client run dev\" \"npm --prefix server run dev\"",
    "dev:client": "npm --prefix client run dev",
    "dev:server": "npm --prefix server run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
